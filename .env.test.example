# LET OP: IS_TEST_DATABASE hoort ALLEEN in .env.test, nooit in productie-.env.
# Kopieer dit bestand naar .env.test en vul de placeholders in met test-Supabase-gegevens.
SUPABASE_URL=# vul in
SUPABASE_ANON_KEY=# vul in
DATABASE_URL=# vul in
ANTHROPIC_API_KEY=# vul in
JWT_SECRET=# vul in
TEACHER_TOKEN=# vul in
PORT=# vul in (aanbevolen: 8080)
IS_TEST_DATABASE=true
